spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:../testdb/cba;AUTO_SERVER=TRUE
  jpa:
    properties:
        hibernate:
            dialect: org.hibernate.dialect.H2Dialect